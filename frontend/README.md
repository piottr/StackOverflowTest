# Frontend

Angular application with PrimeNG for displaying Stack Overflow tags.

## Setup

1. Install Node.js and npm.
2. Run `npm install` in the frontend directory.
3. Run `npm start` to start the development server.

The app will connect to the API at `http://localhost:8080`.